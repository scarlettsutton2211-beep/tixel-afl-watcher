name: Tixel AFL watcher

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check Tixel
        run: |
          echo "Tixel watcher is ready!"
          echo "This workflow will check every 5 minutes."
        
